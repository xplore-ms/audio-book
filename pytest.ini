[pytest]
asyncio_mode = strict

filterwarnings =
    # PyMuPDF (fitz) uses SWIG C-extension types — unfixable in userland, safe to ignore
    ignore::DeprecationWarning:swig
    ignore:builtin type Swig.*has no __module__:DeprecationWarning
